# Runtime dependencies for the event-DB generators and validator.
# Versions are pinned so CI, local `make regenerate`, and Zenodo's
# replication-package rebuild all read from the same substrate.
# Bumping a pin is a deliberate act: run the full test suite and
# verify derived/ + analysis/paper_tables/ regenerate byte-stable
# before committing the bump.
PyYAML==6.0.3
jsonschema==4.26.0
